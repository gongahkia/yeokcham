name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install --yes --no-install-recommends gnupg openssl socat softhsm2
      - if: runner.os == 'macOS'
        run: |
          brew install gnupg openssl socat softhsm
          echo "YEOKCHAM_V4_TEST_OPENSSL=$(brew --prefix openssl)/bin/openssl" >> "$GITHUB_ENV"
          echo "YEOKCHAM_V4_TEST_SOCAT=$(brew --prefix socat)/bin/socat" >> "$GITHUB_ENV"
      - uses: ocaml/setup-ocaml@e89b2ded52a6e13f50162220cf5fe47290162032 # v3.8.0
        with:
          ocaml-compiler: "5.5.0"
          dune-cache: true
      - run: opam install . --deps-only --with-test
      - name: create disposable PKCS#11 token
        shell: bash
        run: |
          token_root="$RUNNER_TEMP/yeokcham-softhsm"
          mkdir -p "$token_root/tokens"
          cat > "$token_root/softhsm2.conf" <<EOF
          directories.tokendir = $token_root/tokens/
          objectstore.backend = file
          log.level = ERROR
          EOF
          export SOFTHSM2_CONF="$token_root/softhsm2.conf"
          softhsm2-util --init-token --free --label yeokcham-v4-test --so-pin 12345678 --pin 1234
          module_path="$(find /usr/local /opt/homebrew /usr/lib -name libsofthsm2.so -type f -print -quit 2>/dev/null)"
          test -n "$module_path"
          echo "SOFTHSM2_CONF=$SOFTHSM2_CONF" >> "$GITHUB_ENV"
          echo "YEOKCHAM_V4_TEST_PKCS11_MODULE=$module_path" >> "$GITHUB_ENV"
      - run: opam exec -- dune build @lint @all
      - run: opam exec -- dune runtest
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: ocaml/setup-ocaml@e89b2ded52a6e13f50162220cf5fe47290162032 # v3.8.0
        with:
          ocaml-compiler: "5.5.0"
      - uses: ocaml/setup-ocaml/lint-fmt@e89b2ded52a6e13f50162220cf5fe47290162032 # v3.8.0
