# TypeScript semantic-sidecar fixtures

These bounded local fixtures exercise declaration rename, declaration move,
declaration replacement, formatting retargeting, duplicate anchors, and parse
failure. They are research inputs, not a claim of TypeScript grammar coverage.
